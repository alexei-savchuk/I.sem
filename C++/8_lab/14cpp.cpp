#include <iostream>
#include <cmath>
using namespace std;
void main()
{
	setlocale(LC_CTYPE, "Russian");
	int count = 5, n = 1, i;
	double t, z, y, x, sum = 0, c=10.1;
	for (i = 0; i < 6; i++) {
		cout << "Введите x" << i << endl;
		cin >> x;
		if (y <= c) {}
		sum = sum + pow(x, 3);
	}
	cout << "Ответ" << y << endl;
}
