try { //код ..
  fgft
    alert ("проверка кода")
    var x=prompt("Любое число");
    
     num1=((8*x)/3); // выражение
 document.writeln(num1); //вывод результата на страницу
  
  } catch (err) { // перхват ошибки
    alert ("ошибка")  //вывести слово "ошибка"
  }
